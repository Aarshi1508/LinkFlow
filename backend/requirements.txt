fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
pydantic==2.7.1
pydantic-settings==2.2.1
email-validator==2.1.1
python-jose[cryptography]==3.3.0
# Used directly (see core/security.py) rather than through passlib -
# passlib 1.7.4 is unmaintained and crashes against bcrypt>=4.1 with
# "AttributeError: module 'bcrypt' has no attribute '__about__'".
# Calling bcrypt's own API removes that broken compatibility layer entirely.
bcrypt==4.2.0
python-multipart==0.0.9
python-dotenv==1.0.1
